using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class AbiltyAdderTestScript : MonoBehaviour
{
    void Update()
    {
        if (Input.GetKeyDown(";")) {
        }
    }
}
